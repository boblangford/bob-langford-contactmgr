
I used Spring Boot to set up the project.  It uses Spring MVC and H2 for the database.
Use maven (version 3.1 is what I used) to build the project.

I used the HTTP Client tool in IntelliJ IDEA to exercise the code.  The queries are in data/rest-calls.http

